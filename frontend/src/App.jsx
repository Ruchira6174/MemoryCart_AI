import React from "react";
import ChatPage from "./pages/chatPage";

/**
 * App — root component for MemoryCart AI.
 * Renders the single-page ChatPage view.
 * Extend here if routing or auth context is added in future.
 */
export default function App() {
  return <ChatPage />;
}
