/**
 * ChatWindow.jsx — reserved for future chat window extraction.
 *
 * Chat rendering is currently self-contained in chatPage.jsx.
 * This stub prevents parse errors if this file is ever imported.
 */
export default function ChatWindow() {
  return null;
}
